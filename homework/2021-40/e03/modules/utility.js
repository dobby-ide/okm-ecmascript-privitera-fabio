export function min(array) {
  return Math.min(...array); //...array gives the values inside of an array therefore they can be treated as values by Math.min
}
export function max(array) {
  return Math.max(...array);
}
